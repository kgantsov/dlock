kind: pipeline
name: default

steps:
- name: build api
  image: plugins/docker
  settings:
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password
    repo: kgantsov/dlock
    context: .
    dockerfile: ./Dockerfile
    auto_tag: true